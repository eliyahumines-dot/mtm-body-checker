# docs/experiments/

Records of runnable experiments/benchmarks and their results — methodology,
inputs used, metrics, conclusions. Raw data, images, and model outputs are
not committed here (see `.gitignore`); link to where they're stored instead.
No experiments have been run yet. See `docs/research/POC_RECOMMENDATION.md`
for the first experiment to run.
